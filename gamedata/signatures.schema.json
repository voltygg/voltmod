{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "voltmod gamedata signatures.jsonc",
  "description": "Platform-specific byte patterns and offsets resolved at runtime by VoltMod::GameData. Fragile: re-verify after every CS2 engine update.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "$schema": { "type": "string" },
    "offsets": {
      "type": "object",
      "description": "Named integer offsets (vtable indexes or byte offsets), per platform.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "anyOf": [{ "required": ["windows"] }, { "required": ["linux"] }],
        "properties": {
          "windows": { "type": "integer" },
          "linux": { "type": "integer" }
        }
      }
    },
    "signatures": {
      "type": "object",
      "description": "Named byte-pattern signatures, per platform.",
      "additionalProperties": {
        "type": "object",
        "additionalProperties": false,
        "anyOf": [{ "required": ["windows"] }, { "required": ["linux"] }],
        "properties": {
          "library": {
            "type": "string",
            "default": "server",
            "description": "Module to scan, without platform prefix/suffix (\"server\" -> server.dll / libserver.so)."
          },
          "windows": { "$ref": "#/definitions/platformSignature" },
          "linux": { "$ref": "#/definitions/platformSignature" }
        }
      }
    }
  },
  "definitions": {
    "platformSignature": {
      "type": "object",
      "additionalProperties": false,
      "required": ["pattern"],
      "properties": {
        "pattern": {
          "type": "string",
          "pattern": "^([0-9A-Fa-f]{2}|\\?\\??)( ([0-9A-Fa-f]{2}|\\?\\??))*$",
          "description": "Space-separated hex bytes; \"?\" or \"??\" is a wildcard byte."
        },
        "offset": {
          "type": "integer",
          "description": "Byte distance from the match to a rel32 displacement (RIP-relative resolve). Omit (or 0) when the match IS the target."
        }
      }
    }
  }
}
