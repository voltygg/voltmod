{#- A labelled Button. `text` is literal or a `{s:var}`; `variant` adds a `Button-<variant>`
    modifier the screen styles. Modifiers use one dash: `--` would read as a server-written
    class family. -#}
{% macro button(id, text, variant="") -%}
<Button id="{{screen}}_{{id}}" class="Button{% if variant %} Button-{{variant}}{% endif %}">
  <Label class="Button-label" text="{{text}}" hittest="false" />
</Button>
{%- endmacro %}
