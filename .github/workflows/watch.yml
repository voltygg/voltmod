name: Watch upstream

on:
  schedule:
    - cron: "0 6 * * *"  # watch, daily
    - cron: "0 4 * * 0"  # prune, weekly
  workflow_dispatch:

permissions:
  contents: write
  pull-requests: write

jobs:
  watch:
    runs-on: ubuntu-latest
    if: github.event.schedule != '0 4 * * 0'
    env:
      GH_TOKEN: ${{ github.token }}
    steps:
      # GITHUB_TOKEN can't open PRs, and its pushes never start CI.
      - uses: actions/create-github-app-token@v2
        id: app-token
        with:
          app-id: ${{ vars.BOT_APP_ID }}
          private-key: ${{ secrets.BOT_APP_PRIVATE_KEY }}

      - uses: actions/checkout@v7
        with:
          token: ${{ steps.app-token.outputs.token }}

      - uses: ./.github/actions/setup-toolchain

      - name: Resolve the upstream tips
        id: watch
        run: python -m tools.release watch

      - name: Open or update the PR
        if: steps.watch.outputs.changed == 'true'
        env:
          GH_TOKEN: ${{ steps.app-token.outputs.token }}
        run: |
          branch=bot/sdk-pins
          title="chore(sdk): follow upstream"
          bot="${{ steps.app-token.outputs.app-slug }}[bot]"
          git config user.name "$bot"
          git config user.email "$bot@users.noreply.github.com"
          git checkout -B "$branch"
          git add recipes/*/conandata.yml
          git commit -m "$title"
          git push --force origin "$branch"
          body=$(git show --stat --format= HEAD)
          gh pr create --head "$branch" --title "$title" --body "$body" || gh pr edit "$branch" --body "$body"

  prune:
    runs-on: ubuntu-latest
    if: github.event.schedule != '0 6 * * *'
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-toolchain
      - name: Prune superseded artifacts
        env:
          CLOUDSMITH_API_KEY: ${{ secrets.CLOUDSMITH_API_KEY }}
        run: python -m tools.release prune --keep 3
