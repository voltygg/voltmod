name: Publish

# Two release paths into the same public Cloudsmith remote:
#
#   push to main touching recipes/**  ->  the SDK packages, then a rebuilt voltmod so a
#                                         consumer resolving the new SDK finds a binary
#   v* tag                            ->  voltmod itself
#
# The SDK path used to be a separate repo, which is why it needed a cross-checkout and a
# "pin voltmod to its newest release" dance to avoid building an untagged tip. Both halves
# now build the working tree, and `voltmod package publish` refuses a v* tag that disagrees
# with version.txt.
#
# Secrets: CLOUDSMITH_USERNAME (an account with Write on the repo) and CLOUDSMITH_API_KEY
# (that account's API key - entitlement tokens are read-only).

on:
  push:
    branches: [main]
    paths: ["recipes/**"]
    tags: ["v*"]
  workflow_dispatch:

jobs:
  sdk:
    name: SDK packages (${{ matrix.name }})
    if: "!startsWith(github.ref, 'refs/tags/')"
    strategy:
      fail-fast: false
      matrix:
        include:
          - name: linux-steamrt
            os: ubuntu-latest
            container: registry.gitlab.steamos.cloud/steamrt/sniper/sdk:latest
          - name: windows-msvc
            os: windows-latest
            container: ""
    runs-on: ${{ matrix.os }}
    container: ${{ matrix.container || null }}
    env:
      CLOUDSMITH_USERNAME: ${{ secrets.CLOUDSMITH_USERNAME }}
      CLOUDSMITH_API_KEY: ${{ secrets.CLOUDSMITH_API_KEY }}
    steps:
      - uses: actions/checkout@v7

      - uses: ./.github/actions/setup-toolchain

      - name: Build and upload the SDK packages
        run: voltmod package publish sdk

      # Linux only: it is the deploy target, and a Windows Release voltmod.lib is ~208 MB
      # of embedded CodeView. Windows and Debug consumers build the kit themselves.
      - name: Rebuild voltmod against them
        if: runner.os == 'Linux'
        run: voltmod package publish kit --no-lockfile

  tag:
    name: Tag the SDK versions
    needs: sdk
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-toolchain
      - name: Tag each pinned version
        run: voltmod package tag

  kit:
    name: Release voltmod
    if: startsWith(github.ref, 'refs/tags/v')
    runs-on: ubuntu-latest
    container:
      image: registry.gitlab.steamos.cloud/steamrt/sniper/sdk:latest
    env:
      CLOUDSMITH_USERNAME: ${{ secrets.CLOUDSMITH_USERNAME }}
      CLOUDSMITH_API_KEY: ${{ secrets.CLOUDSMITH_API_KEY }}
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-toolchain
      - name: Build and upload voltmod
        run: voltmod package publish kit

  smoke:
    name: Consume from the remote (anonymous)
    needs: [kit]
    runs-on: ubuntu-latest
    container:
      image: registry.gitlab.steamos.cloud/steamrt/sniper/sdk:latest
    steps:
      - uses: actions/checkout@v7
      - uses: ./.github/actions/setup-toolchain

      # No login. That the remote serves anonymously is the property under test, and it
      # is the one thing a publishing job cannot check about itself.
      - name: Fresh consume of voltmod from the remote
        env:
          CONAN_HOME: ${{ runner.temp }}/conan-fresh
        run: |
          conan profile detect --force >/dev/null 2>&1 || true
          conan config install ./conan
          # --build=!<ours>: only voltmod and the SDKs are claimed to be prebuilt. Conan
          # Center has no binaries for the gcc-14 / old-libstdc++-ABI profile, so cpr and
          # friends legitimately compile here.
          conan test test_package "voltmod/$(cat version.txt)" \
            -pr:a conan/profiles/linux-steamrt.txt -s build_type=Release \
            --build=missing '--build=!voltmod/*' \
            '--build=!hl2sdk-cs2/*' '--build=!metamod-source/*'
